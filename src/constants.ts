export const APP_TITLE = 'NAKSIR.AI GO PREMIUM — Daily Predictions';

export const API_BASE_URL =
  import.meta.env.VITE_API_BASE_URL || 'https://api.example.com'; // override via env in production;
